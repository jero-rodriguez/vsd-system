{
  "name": "vsd-system",
  "version": "0.0.5",
  "description": "CSS compiler for the VsD system",
  "scripts": {
    "watch": "npx sass src/scss/vsd-system.scss css/vsd-system.css --style=expanded --source-map --watch",
    "clean": "rimraf dist",
    "build": "node tools/build.mjs",
    "zip": "node tools/zip.mjs",
    "version:patch": "standard-version --release-as patch",
    "version:minor": "standard-version --release-as minor",
    "version:major": "standard-version --release-as major",
    "publish:patch": "npm run version:patch && git push origin main --follow-tags",
    "publish:minor": "npm run version:minor && git push origin main --follow-tags",
    "publish:major": "npm run version:major && git push origin main --follow-tags",
    "release": "npm run build && npm run zip"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Jeronimo Rodriguez",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": "^13.346.0-beta.20250812191140",
    "archiver": "^7.0.1",
    "fs-extra": "^11.3.2",
    "rimraf": "^6.1.2",
    "sa": "^0.2.50",
    "sass": "^1.96.0",
    "standard-version": "^9.5.0",
    "typescript": "^5.9.3"
  }
}
